export default function Navbar() {
  return (
    <header style={{ padding: '16px 24px', background: 'var(--color-cream)', borderBottom: '1px solid var(--color-border)' }}>
      <nav aria-label="Main navigation">
        <span style={{ fontFamily: 'var(--font-primary)', color: 'var(--color-primary)' }}>Uniblueprint</span>
      </nav>
    </header>
  )
}
